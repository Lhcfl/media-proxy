# Misskey media proxy configuration (Bun rewrite).
# Point MISSKEY_MEDIA_PROXY_CONFIG at this file, or drop it in the working
# directory as config.toml. NixOS can generate it with pkgs.formats.toml.

userAgent = "MisskeyMediaProxy/0.0.0"

# Private network ranges that may be reached (empty = block all).
allowedPrivateNetworks = []

# Maximum download size in bytes.
maxSize = 262144000

# CORS
"Access-Control-Allow-Origin" = "*"
"Access-Control-Allow-Headers" = "*"

# CSP
"Content-Security-Policy" = "default-src 'none'; img-src 'self'; media-src 'self'; style-src 'unsafe-inline'"

# Forward proxy (Bun also honours HTTP_PROXY / HTTPS_PROXY / ALL_PROXY).
# proxy = "http://127.0.0.1:3128"

# Bun-specific tuning:
# Cap on simultaneous image conversions (default 4).
# maxConcurrentConversions = 4
# Whole-download timeout in milliseconds (default 60000).
# downloadTimeoutMs = 60000
